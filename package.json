{
  "name": "@crownthrive/chlom-chain-evidence-fabric",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Governed provider-neutral blockchain RPC, Google Blockchain Analytics, evidence, REST, and MCP runtime for CHLOM.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test tests/*.test.mjs",
    "certify": "npm test && npm run build"
  },
  "dependencies": {
    "@vercel/oidc": "3.8.0",
    "google-auth-library": "11.0.2"
  }
}
